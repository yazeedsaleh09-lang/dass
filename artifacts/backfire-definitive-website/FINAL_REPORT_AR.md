# BACKFIRE — التقرير النهائي لجلسة الموقع (2026-07-21)

الفرع: `feat/backfire-vertical-slice` · HEAD: `98d10cc` · **لا commit / لا push / لا reset / لا clean**.

> تحديث: هذا التقرير يستبدل نسخة سابقة كانت تصف الأسماء المخترعة وتؤجّل العمل البصري. النسخة السابقة لم تعد دقيقة.

---

## 1. ما أُنجِز في هذه الجلسة

1. **تصحيح أسماء الأطوار الخمسة** إلى أسماء الأنمي الصحيحة (التغيير الحرج المطلوب).
2. **إعادة بناء صفحة الأسئلة `/faq`** لتغطية مجموعة الأسئلة المطلوبة كاملة (11 سؤال لعب + 4 خدمات)، بمجموعتين وعناوين، وإجابات مُتحقَّقة من التنفيذ.
3. **تدقيق نسخة الأطوار السعودية** لتطابق الاتجاهات الدرامية المطلوبة (السطر + الخبيئة لكل عالم).
4. **QA بصري حقيقي** عبر متصفح: مسح overflow على كل المسارات عند 320px، فحص H1 وRTL وconsole، ولقطات Chrome headless محفوظة في `after/`.
5. **إعادة توليد الحزمة** (`bundle.js`) بحيث لا تحتوي أي اسم مخترع.
6. توثيق فعلي: `FUTURE_MODE_RESEARCH_AR.md`, `MODES_PAGE_AR.md`, `SAUDI_COPY_SYSTEM_AR.md`, `QA_AR.md`.

الهيرو والبطل المنتج وسرد how-to-play الطولي كانت **مبنية فعليًا وبجودة قوية** من جلسات سابقة، وتحقّقتُ منها بصريًا في هذه الجلسة (لقطات مرفقة) بدل إعادة بنائها بلا داعٍ والمخاطرة بكسر 32 اختبارًا.

## 2. تصحيح أسماء الأنمي

**أُزيلت** بالكامل (0 نتيجة في المصدر والحزمة):

- الحصار الأخير · اختبار الثقة · حافة الخسارة · أمر الانقلاب · أثر اللعنة

**تُستخدم الآن** (مع الحفاظ على المعرّفات الثابتة):

| المعرّف | الاسم |
|--------|------|
| `siege` | هجوم العمالقة |
| `classroom` | لعبة الأصدقاء |
| `orbit` | كايجي |
| `blackout` | كود غياس |
| `hotel` | جوجوتسو كايسن |

كلها: قريبًا · غير قابلة للّعب · زر «قريبًا» مُعطّل · فن BACKFIRE تجريدي أصلي · بلا أي شخصية أو شعار أو لقطة أنمي · بلا ادّعاء ترخيص/رعاية.

## 3. التغييرات البصرية

- صفحة `/faq`: مجموعتان («اللعب» / «الحساب والخدمات») بعناوين `faq-group` مُنسّقة، 15 سؤالًا، أول عنصر مفتوح.
- الأطوار: أسماء الأنمي تظهر على البطاقات والتفاصيل، مع الأغلفة التجريدية الأصلية كما هي.
- لا تغيير على مشاهد SVG القوية القائمة (هيرو/منتج/عواقب).

## 4. تغييرات النسخة السعودية

- أسطر الأطوار الخمسة عُدّلت لتطابق الاتجاهات: مثال `siege` → «الجدار يعتمد على قراراتكم… وواحد منكم يقدر يطيّحه.»
- FAQ بلهجة سعودية طبيعية وإجابات صادقة (الضيف، الشراء، المدة، إعادة الاتصال، سرية القرار، اختلاف عدد الجولات بين الأطوار).

## 5. المسارات والدقات المُختبَرة

المسارات: `/`, `/create`, `/join`, `/how-to-play`, `/modes`, `/modes/mode?id=…`, `/faq`, `/about`, `/pricing`, 404.
الدقات: **320×568، 390×844، 1280×800** (+ لقطات طويلة). التفاصيل في `QA_AR.md`.

## 6. البناء والفحص والاختبارات

- Build: **نجح**.
- Typecheck: **نجح، بلا أخطاء**.
- Tests: **32 passed · 0 failed · 0 skipped**.

## 7. الملفات المعدّلة والجديدة (هذه الجلسة)

**معدّلة:**
- `apps/dasssite/src/modes/catalog.ts` — الأسماء + الأسطر.
- `apps/dasssite/src/product/pages.ts` — إعادة بناء `/faq`.
- `apps/dasssite/src/product/product-css.ts` — نمط `.faq-group`.
- `apps/dasssite/public/bundle.js` + `.map` — إعادة توليد.
- `.claude/launch.json` — إضافة إعداد خادم الموقع.

**جديدة:** `artifacts/backfire-definitive-website/{FUTURE_MODE_RESEARCH_AR, MODES_PAGE_AR, SAUDI_COPY_SYSTEM_AR, QA_AR}.md` + لقطات `after/`.

> (تغييرات المصدر الأخرى في `git status` من جلسة سابقة، محفوظة كما هي — لم تُمسّ.)

## 8. مخاطر متبقية

- أداة اللقطة المدمجة تنتهي مهلتها (حلقة rAF في مشاهد الموقع)؛ استُخدم Chrome headless كبديل ناجح. يُنصح بمراجعة أداء حلقة الحركة لاحقًا.
- الحساب والدفع محاكاة محلية صريحة فقط — لا مزود إنتاج موصول (مُعلَن بصدق في الموقع وسليم).
- إعادة تصميم بصرية أعمق للأطوار المستقبلية (وراء الأغلفة الحالية) تبقى فرصة تحسين اختيارية، غير حاجزة.

## 9. حالة Git

- الفرع: `feat/backfire-vertical-slice` · HEAD: `98d10cc`.
- **لا commit · لا push · لا reset · لا تبديل فرع · لا git clean · لا حذف مدمّر.** شجرة العمل محفوظة.
